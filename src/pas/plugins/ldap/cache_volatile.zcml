<configure xmlns="http://namespaces.zope.org/zope">
  <!-- BBB, DEPRECATED
       We implemented another caching strategy based on plone.memoize, which
       caches the results of expensive ugm().users and ugm().groups methods
       instead of lightweight ugm() instance itself.
  -->
</configure>
